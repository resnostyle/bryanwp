---
/** Warm, minimal canvas — one accent wash, not a gradient stack. */
---

<div aria-hidden="true" class="pointer-events-none fixed inset-0 -z-10 bg-stone-950">
	<div
		class="absolute inset-0 bg-[radial-gradient(ellipse_70%_50%_at_100%_0%,rgba(245,158,11,0.12),transparent)]"
	>
	</div>
</div>
